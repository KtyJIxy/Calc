#:import fm Calc.functions_main

<MainScreen>:
    id: calc_scr
    display: entry
    rows: 6

    BoxLayout:
        TextInput:
            id: entry

    BoxLayout: 
        Button: 
            text: "7"
            on_press: entry.text += self.text 
        Button: 
            text: "8"
            on_press: entry.text += self.text 
        Button: 
            text: "9"
            on_press: entry.text += self.text 
        Button:
            text: "+"
            on_press: fm.addition()
            # Doesnt' work. Either function has to wait for second input or "+" is just a symbot to be rarsed.
            #TODO: so 2 ways: new func or even new parser.